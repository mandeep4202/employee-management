apiVersion: v1
kind: Service
metadata:
  name: employeeservice-svc
spec:
  selector:
    app: employeeservice
  ports:
    - protocol: TCP
      port: 9090
      targetPort: 8080
      nodePort: 30080
  type: NodePort
